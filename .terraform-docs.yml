formatter: markdown table

sections:
  show: [requirements, providers, resources, inputs, outputs]

content: |-
  {{ .Header }}

  ## Usage

  ### Basic

  ```hcl
  {{ include "examples/basic/main.tf" }}
  ```

  ### Complete

  ```hcl
  {{ include "examples/complete/main.tf" }}
  ```

  {{ .Requirements }}
  {{ .Providers }}
  {{ .Resources }}
  {{ .Inputs }}
  {{ .Outputs }}

output:
  file: README.md
  mode: inject
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->

sort:
  enabled: true
  by: required

settings:
  anchor: true
  default: true
  description: true
  required: true
  sensitive: true
  type: true
